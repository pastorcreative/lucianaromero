---
import LogoGrid from './LogoGrid.astro';
import { fetchConfiguracion } from '../lib/payload';

const { marcas } = await fetchConfiguracion();

const items = marcas.map(m => ({
    name: m.nombre,
    url:  m.url,
    src:  m.logoSrc,
}));
---

<LogoGrid label="BRANDS" items={items} />
