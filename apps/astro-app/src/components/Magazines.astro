---
import LogoGrid from './LogoGrid.astro';
import { fetchConfiguracion } from '../lib/payload';

const { revistas } = await fetchConfiguracion();

const items = revistas.map(r => ({
    name: r.nombre,
    url:  r.url,
    src:  r.logoSrc,
}));
---

<LogoGrid label="MAGAZINES" items={items} />
